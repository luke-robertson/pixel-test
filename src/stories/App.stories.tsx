import { storiesOf } from '@storybook/react';
import * as React from 'react';
import App from '../App';

// storiesOf('app', module).addWithVisreg('Cross Platform Promotion', () => {
//   return <App />;
// });

export const app = () => <App />
